/**\n * ENHANCED BOOTSTRAP V2 - With Autonomy System\n * Loads and starts Kamara Temple OS V2 + Autonomy + ORA Bridge\n */\n\nimport { KamaraEngineV2 } from '../core/engineV2.js';\nimport { BridgeSystem } from '../core/bridge.js';\nimport { AutonomyDashboard } from '../system/autonomyDashboard.js';\n\nclass SystemBootstrapV2 {\n  static async initialize(config = {}) {\n    console.log('🟣 KAMARA TEMPLE OS V2 - INITIALIZATION SEQUENCE');\n    console.log('════════════════════════════════════════════════════════════');\n    \n    // Create engine with autonomy\n    const engine = new KamaraEngineV2();\n    await engine.init(config);\n    \n    // Create bridge\n    const bridge = new BridgeSystem(engine);\n    \n    // Create dashboard\n    const dashboard = new AutonomyDashboard(engine);\n    \n    // Expose to global scope\n    if (typeof window !== 'undefined') {\n      window.System = engine;\n      window.Bridge = bridge;\n      window.Dashboard = dashboard;\n      window.KamaraEngineV2 = KamaraEngineV2;\n    }\n    \n    console.log('✨ Autonomy System: INITIALIZED');\n    console.log('🔔 Event System: READY');\n    console.log('📊 Dashboard: ACTIVE');\n    console.log('\\n🌟 System Ready');\n    console.log('Available: window.System, window.Bridge, window.Dashboard');\n    \n    return { engine, bridge, dashboard };\n  }\n\n  /**\n   * Print autonomy dashboard\n   */\n  static printDashboard(dashboard) {\n    console.clear();\n    console.log(dashboard.toASCII());\n  }\n}\n\nexport { SystemBootstrapV2 };\n