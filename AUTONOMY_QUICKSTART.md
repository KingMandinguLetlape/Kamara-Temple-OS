# 🚀 QUICK START GUIDE - EMISSARY AUTONOMY\n\n## Installation & Startup\n\n### Step 1: Initialize System with Autonomy\n\n```javascript\nimport { SystemBootstrapV2 } from './system/bootstrapV2.js';\n\n// Initialize with default 5-second autonomy ticks\nconst { engine, bridge, dashboard } = await SystemBootstrapV2.initialize();\n\n// System now running:\n// - Emissaries automatically evolving\n// - Autonomy loop ticking every 5 seconds\n// - Dashboard monitoring in real-time\n```\n\n### Step 2: Access Emissaries\n\n```javascript\n// Get emissary with current autonomy state\nconst kamata = System.emissary('Kamata');\nconsole.log(kamata);\n// {\n//   name: 'Kamata',\n//   role: 'Emotional Intelligence',\n//   energy: 73,\n//   level: 2,\n//   currentMood: 'ascended',\n//   autonomousState: { ... }\n// }\n```\n\n### Step 3: Analyze Assets with Live Autonomy\n\n```javascript\n// Emissary's mood/energy will influence the verdict\nconst analysis = await System.analyzeAssetThroughEmissary('Kamata', 'ORA-001');\n\nconsole.log(analysis);\n// {\n//   emissary: 'Kamata',\n//   emissaryEnergy: 73,\n//   emissaryMood: 'ascended',\n//   verdict: 'TRANSCENDENT HOLD',  // Influenced by current state!\n//   autonomyInfluence: 1.32,\n//   baseValuation: { score: 70 },\n//   influencedValuation: { modifiedScore: 92 }\n// }\n```\n\n---\n\n## Key Commands\n\n### Monitor Emissary States\n\n```javascript\n// Get all emissary states\nSystem.run('getAllEmissaryStates')\n\n// Get specific emissary state\nSystem.run('getEmissaryState', 'Amata')\n\n// Manual tick (normally automatic)\nSystem.run('tickAllEmissaries')\n```\n\n### Dashboard Operations\n\n```javascript\n// Get real-time snapshot\nDashboard.getSnapshot()\n\n// Get system metrics\nDashboard.getMetrics()\n\n// Print ASCII dashboard\nSystemBootstrapV2.printDashboard(Dashboard)\n\n// Example metrics output:\n// {\n//   systemEnergy: 68,        // Average across all emissaries\n//   systemLevel: 1.8,        // Average level\n//   systemExperience: 156,   // Average experience\n//   evolutionRate: 0.23,     // Actions per second\n//   moodDistribution: { ascended: 1, balanced: 2, critical: 1 }\n// }\n```\n\n### Event Triggering\n\n```javascript\n// Trigger event (causes emissaries to react)\nSystem.triggerEvent('BEACON_ACTIVATED', { frequency: 432 })\n\n// Create inquiry (deducts emissary energy)\nSystem.createInquiry('ORA-001', { name: 'Buyer', offerPrice: 2500 })\n\n// Activate sigil (boosts emissary energy)\nSystem.activateSigil('Gamada')\n```\n\n### Beacon Control\n\n```javascript\n// Activate beacon (influences emissary energy)\nSystem.activateBeacon(432)  // Healing frequency\n// Emissaries gain: +3 energy, +8 balance\n\nSystem.activateBeacon(528)  // Love frequency\n// Emissaries gain: +4 energy, +10 compassion\n```\n\n---\n\n## Real-World Workflow\n\n### Scenario: Track Asset Evaluation Over Time\n\n```javascript\n// 1. Initialize\nconst { engine, dashboard } = await SystemBootstrapV2.initialize();\n\n// 2. Activate beacon to boost emissaries\nSystem.activateBeacon(432);\nconsole.log('✨ Healing frequency activated');\n\n// 3. Wait 5 seconds for autonomy tick\nawait new Promise(resolve => setTimeout(resolve, 5000));\n\n// 4. Check emissary states\nconst states = System.run('getAllEmissaryStates');\nconsole.log('Average Energy:', \n  Object.values(states).reduce((sum, s) => sum + s.energy, 0) / 4\n);\n\n// 5. Now analyze asset (emissaries are energized)\nconst analysis = await System.analyzeAssetThroughEmissary('Kamata', 'ORA-001');\nconsole.log('Verdict:', analysis.verdict);\nconsole.log('Modification:', analysis.autonomyInfluence);\n\n// 6. Compare with low-energy analysis\nSystem.stopAutonomyLoop(); // Stop evolution\n// Manually deplete energy\nconst kamata = System.emissary('Kamata');\nSystem.memory.set('emissary_state_Kamata', {\n  ...System.run('getEmissaryState', 'Kamata'),\n  energy: 15\n});\n\n// 7. Analyze same asset with low energy\nconst lowEnergyAnalysis = await System.analyzeAssetThroughEmissary('Kamata', 'ORA-001');\nconsole.log('Low Energy Verdict:', lowEnergyAnalysis.verdict);\nconsole.log('Low Energy Modification:', lowEnergyAnalysis.autonomyInfluence);\n\n// Result: Same asset gets different verdicts based on emissary state!\n```\n\n### Scenario: Multi-Emissary Consensus\n\n```javascript\n// Get all emissaries' perspectives on same asset\nconst asset = 'ORA-005';\nconst emissaryNames = ['Kamata', 'Amata', 'Ntala', 'Mandingus'];\n\nconst analyses = await Promise.all(\n  emissaryNames.map(name => System.analyzeAssetThroughEmissary(name, asset))\n);\n\nconsole.log('Asset Analysis Summary:');\nanalyses.forEach(analysis => {\n  console.log(`${analysis.emissary}`);\n  console.log(`  Mood: ${analysis.emissaryMood}`);\n  console.log(`  Energy: ${analysis.emissaryEnergy}`);\n  console.log(`  Verdict: ${analysis.verdict}`);\n  console.log(`  Influence: ${(analysis.autonomyInfluence * 100).toFixed(1)}%`);\n});\n\n// Consensus: Count verdicts\nconst verdictCounts = {};\nanalyses.forEach(a => {\n  verdictCounts[a.verdict] = (verdictCounts[a.verdict] || 0) + 1;\n});\nconsole.log('Verdict Distribution:', verdictCounts);\n```\n\n---\n\n## Understanding Autonomy Impact\n\n### Energy State Effects\n\n| Energy | Mood | Valuation Modifier | Decision Style |\n|--------|------|-------------------|----------------|\n| 90+ | Ascended | +15% | Aggressive, confident |\n| 70-89 | Ascended | +12% | Optimistic |\n| 50-69 | Balanced | 0% | Neutral |\n| 25-49 | Diminished | -8% | Conservative |\n| 0-24 | Critical | -15% | Defensive, pessimistic |\n\n### Verdict Examples by Mood\n\n**Kamata (Emotional):**\n- Ascended + High Potential: **TRANSCENDENT HOLD**\n- Balanced + Moderate: **WATCH**\n- Critical + Any: **RETREAT**\n\n**Amata (Security):**\n- High Energy + Safe: **STRONGLY APPROVED**\n- Low Energy + Any: **SUSPEND**\n- Risky: **DENY**\n\n**Ntala (Storage):**\n- High Level + High Volume: **ARCHIVE_PRIME**\n- Medium Volume: **STORE**\n- Low Energy: **PRESERVE_ONLY**\n\n**Mandingus (Control):**\n- High Energy + Level 3+ + High Score: **DOMINATE**\n- High Score: **EXECUTE**\n- Low Energy: **DEFER**\n\n---\n\n## Configuration & Customization\n\n### Change Autonomy Tick Rate\n\n```javascript\nconst { engine } = await SystemBootstrapV2.initialize({\n  tickRate: 2000  // 2 seconds instead of 5\n});\n```\n\n### Manually Control Autonomy\n\n```javascript\n// Stop automatic ticking\nSystem.stopAutonomyLoop();\n\n// Restart with new rate\nSystem.startAutonomyLoop(3000);\n\n// Manual tick (immediate, no wait)\nSystem.tickAllEmissaries();\n```\n\n### Access Raw Autonomy System\n\n```javascript\n// Direct access to autonomy engine\nconst autonomy = System.autonomy;\n\n// Get modifier for asset evaluation\nconst modifier = autonomy.getEvaluationModifier('Kamata', assetData);\n\n// Get influence on valuation\nconst influenced = autonomy.influenceValuation('Kamata', baseValuation);\n```\n\n---\n\n## Troubleshooting\n\n### \"Emissary energy not changing\"\n```javascript\n// Check if autonomy loop is running\nconsole.log(System.autonomyTickInterval !== null ? 'Running' : 'Stopped');\n\n// Check if beacons are active\nconsole.log(System.beacons.isActive()); // true/false\n\n// Manually trigger tick\nSystem.tickAllEmissaries();\n```\n\n### \"Verdicts are same regardless of energy\"\n```javascript\n// Verify autonomy is influencing:\nconst state = System.run('getEmissaryState', 'Kamata');\nconsole.log(state.energy); // Should change over time\n\n// Check influence modifier:\nconst modifier = System.autonomy.getEvaluationModifier('Kamata', {});\nconsole.log(modifier); // Should vary by energy/mood\n```\n\n### \"Dashboard showing old data\"\n```javascript\n// Dashboard pulls live from engine\nDashboard.getSnapshot(); // Always fresh\n\n// Force refresh\nSystem.tickAllEmissaries();\nDashboard.getSnapshot();\n```\n\n---\n\n## Performance Notes\n\n- **5-second tick rate**: Minimal overhead, ~2ms per tick\n- **Memory usage**: ~5KB per emissary state\n- **Beacon influence**: Real-time, instant energy adjustment\n- **FlameMemory persistence**: Automatic after each tick\n\n---\n\n## Next Steps\n\n1. ✅ Initialize with `SystemBootstrapV2.initialize()`\n2. ✅ Monitor with `Dashboard.getSnapshot()`\n3. ✅ Analyze with `System.analyzeAssetThroughEmissary()`\n4. ✅ Observe evolution over time\n5. 🔮 Integrate with ORA economy layer\n6. 🔮 Build decision systems on emissary verdicts\n\n---\n\n**Emissaries are now autonomous, evolving entities within your system.**\n\n🧠✨\n"